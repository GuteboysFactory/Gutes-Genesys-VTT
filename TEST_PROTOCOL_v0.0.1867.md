# Forge transfers — v0.0.1867

1. Filter Library to a small set, export JSON and import it. Confirm independent entries in My Adversary Templates and unchanged sources.
2. Cancel import; verify no writes. Re-import explicitly creates more copies.
3. Invalid JSON/version or >10 MB: error and no import. Partial persistence failures report the successful count; do not re-import blindly.
4. Create NPC, accept placement: hidden unlinked token at scene centre, portrait retained. Move with native controls.
5. Accept encounter handoff: token-specific NPC participant with zero initiative; roll normally. Verify two copies of the same NPC remain separate participants and Nemesis limits remain bounded.
6. Cancel either prompt: Actor/token already saved remains, no unwanted participant.
7. Choose world NPC in Library and use Place selected world NPC. No active scene: no placement.
8. Switch scene before accepting placement: rejected. Check as secondary GM/player: denied.

Images are not bundled: copy image assets separately when moving worlds. Special abilities remain subject to existing manual exceptions. Live Foundry QA is pending.
